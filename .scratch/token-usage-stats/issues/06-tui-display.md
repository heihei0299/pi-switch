# 06 — TUI 展示

**What to build:** TUI 统计视图追加两行：累计输入/输出 token 总数与缓存命中率百分比。数据来自统计聚合结果，与 WebUI 同源。

**Blocked by:** 04 — 聚合扩展

**Status:** resolved

- [ ] TUI 统计页显示累计输入 token、累计输出 token、缓存命中率
- [ ] 无 token 数据时显示 `-`，不崩溃、不错位
- [ ] 与 WebUI 数字同源一致
