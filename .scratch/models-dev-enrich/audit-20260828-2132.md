# Issue 审计报告：models-dev-enrich

审计时间：2026-08-28 21:32（精确到分钟）｜基线：无历史基线

## 结论总览
- 阻断项：0 项 / 非阻断项：2 项 → 通过
- 每维最严重问题：维度1：无 ｜ 维度2：无 ｜ 维度3：无 ｜ 维度4：README 未提及模型目录 enrich、模型目录 Provider 输入框（非阻断，功能已实现但文档未同步）
- 与上次审计对比：本次为首份报告

## 维度 1 · 完成度（逐票）

### 01 — 目录缓存基础设施（commit e606941）
- [x] `GET https://models.dev/api.json` 10s 超时，成功后原子写入 `~/.pi-switch/cache/models-dev.json`（`config_dir()` 推导，与 `backups` 同级） — 满足。证据：`src-rust/catalog.rs:118-133` `fetch_catalog_from_network` `timeout(Duration::from_secs(10))`；`src-rust/catalog.rs:54-73` `write_catalog_atomic` 创建父目录+tmp+rename；`src-rust/catalog.rs:38-41` `catalog_cache_path()` `config_dir().join("cache").join("models-dev.json")`；提交 `e606941`
- [x] 暴露 `get_or_refresh_catalog()`：TTL 24h 内命中直接读缓存，过期或无缓存时尝试网络，失败回退到过期缓存并 warning，无缓存且失败则跳过 enrich — 满足。证据：`src-rust/catalog.rs:84-129` `CATALOG_TTL_SECS=86400` `is_cache_fresh` `get_or_refresh_catalog_inner_with_warning`；测试 `catalog::tests::get_or_refresh_returns_fresh_cache_without_fetching` `get_or_refresh_fetches_when_stale_and_updates_cache` `get_or_refresh_falls_back_to_stale_cache_on_fetch_failure` `get_or_refresh_returns_none_when_no_cache_and_fetch_fails`；提交 `e606941`
- [x] 目录 payload 全量缓存不切片，约 4.4MB，读写原子性与并发安全 — 满足。证据：`src-rust/catalog.rs:54-73` 原子写；`src-rust/catalog.rs:113-133` 全量 `Value` 缓存；spec 声明 4.4MB，代码不切片；提交 `e606941`
- [x] 测试以 fixture `api.json` 片段注入，不依赖真实网络，覆盖命中/过期/网络失败降级分支 — 满足。证据：`src-rust/catalog.rs:324-517` 12 测试用 `temp_path` `write_fixture` `set_mtime` 注入 fixture；`cargo test --lib` 263 passed；提交 `e606941`
- [x] 尊重 ADR 0005 与 CONTEXT.md 术语（模型目录 / 模型元数据） — 满足。证据：`src-rust/catalog.rs` 注释与日志均使用“模型目录”；`CONTEXT.md:193-200` 术语定义；`docs/adr/0005-models-dev-as-model-metadata-source.md` 已创建；提交 `e606941`

### 02 — Profile 映射字段扩展（commit 517c269）
- [x] `ProviderProfile` 新增可选字段 `modelsDevProvider?: string`（`src-rust/config.rs`，`Option<String>`，`skip_serializing_if`），`webui/src/types.ts` 同步 — 满足。证据：`src-rust/config.rs:246` `modelsDevProvider: Option<String>` `rename="modelsDevProvider"`；`webui/src/types.ts:7` 同步；提交 `517c269`
- [x] 映射优先级：`modelsDevProvider` 显式值优先，未填时按 preset→目录 key 推断（openrouter/anthropic/deepseek/openai/siliconflow 等），推断失败跳过 enrich 不报错 — 满足。证据：`src-rust/config.rs:312-340` `resolve_models_dev_provider` `preset_to_models_dev_key`；`src-rust/catalog.rs:28-34` `resolve_catalog_provider` 委托；测试 `catalog::tests::resolve_catalog_provider_prefers_explicit_over_preset` `infer_catalog_provider_key_maps_known_presets` `resolve_catalog_provider_none_when_no_mapping`；提交 `517c269`
- [x] `validate_provider_profile` 中对 `modelsDevProvider` 仅 warning（未知 key 跳过，不阻断保存，避免目录新增 provider 时硬编码滞后） — 满足。证据：`src-rust/config.rs:412-438` `validate_provider_profile` 对 `modelsDevProvider` 产生 `ValidationIssue::Warning`；测试 `config::tests::models_dev_provider_warning_for_unknown_key`；提交 `517c269`
- [x] 现有 profile 无该字段时反序列化兼容（`None`），round-trip 不丢字段 — 满足。证据：`src-rust/config.rs:246` `default` `skip_serializing_if`；测试 `config::tests::models_dev_provider_round_trips_and_preserves_explicit_value` `models_dev_provider_deserializes_none_when_missing`；提交 `517c269`
- [x] 术语与文案统一使用“模型目录 / 模型元数据” — 满足。证据：`src-rust/config.rs` 日志与注释使用该术语；提交 `517c269`

### 03 — Fetch 链路 enrich 核心（commit bde27bf）
- [x] 在 `fetch_models` / `update_provider_models` 路径内新增 `enrich_models_from_catalog(profile, ids)`，复用 `get_or_refresh_catalog()`，触发范围：TUI/WebUI Fetch Models 与新增/编辑 profile 的自动发现；`sync_gateway_to_pi` 间接受益 — 满足。证据：`src-rust/ops.rs:240-430` `enrich_models_with_catalog` `enrich_models_from_catalog`；`src-rust/ops.rs:520-560` `try_enrich_from_cache_sync` 供 `upsert_profile` `update_provider_models` 同步路径复用；`src-rust/ops.rs:1135-1200` `upsert_profile` 调用 `try_enrich_from_cache_sync_with_stats`；提交 `bde27bf`；后继 `453aeb2` 未改变触发范围
- [x] 字段映射按分字段策略：`limit.context→contextWindow`、`limit.output→maxTokens`、`cost.input/output/cache_read→cost.input/output/cacheRead`（$/1M 直接透传，仅外层 cost，`tiers/context_over_200k` 不展开）、`reasoning→reasoning`、`modalities.input→input` 按目录覆盖，`name` 仅缺省时补齐 — 满足。证据：`src-rust/ops.rs:193-240` `enrich_single_entry` 映射 `limit.context` `limit.output` `cost` 外层 `modalities.input` `name` 仅 `is_none` 时补齐；测试 `ops::enrich_tests::enrich_field_mapping_correctness` `enrich_name_retains_when_not_missing` `enrich_ignores_tiers_and_context_over_200k`；提交 `bde27bf`
- [x] 列表语义：仅 enrich 已有 `profile.models` 中的 id，目录有但本地无的不自动新增（需勾选后才新增）；`extra/compat/headers/thinkingLevelMap` 不由目录覆盖 — 满足。证据：`src-rust/ops.rs:243-260` 注释“仅 enrich 已有 models 中的 id”；`enrich_single_entry` 不触及 `extra/compat/headers/thinkingLevelMap`；测试 `ops::enrich_tests::enrich_not_auto_add_catalog_extra` `enrich_extra_fields_not_overwritten`；提交 `bde27bf`
- [x] 未命中策略：目录无该 id 或字段缺失时保留本地原值，不清 cost/limit，私有/自建模型无报错 — 满足。证据：`src-rust/ops.rs:260-285` `find_in_catalog_map` 未命中则保留原 `ModelEntry`；测试 `ops::enrich_tests::enrich_miss_retains_original` `enrich_none_catalog_retains_original`；提交 `bde27bf`
- [x] 手工 `pi-switch provider models <name> ...` 显式列表不受目录约束（不要求 id 在目录中） — 满足。证据：`src-rust/ops.rs:240` 注释与 `enrich_models_with_catalog` 仅对已有 models enrich，未校验存在性；spec Out of Scope 亦声明；测试覆盖未命中保留；提交 `bde27bf`
- [x] 测试覆盖 enrich 字段映射、分字段覆盖、未命中保留、不自动新增分支 — 满足。证据：`src-rust/ops.rs:520-800` 9+ 测试；`cargo test --lib` 263 passed；提交 `bde27bf`

### 04 — 可观测与降级 polish（commit 64c05c1，后继 869edb1 全局回退补强）
- [x] enrich 结果 `enriched=N skipped=M failed=K` 合并到现有 Fetch 成功 toast 与 `web.rs` 响应中，`skipped` 为目录未覆盖数，`failed` 为网络/解析失败数，失败原因写入日志 — 满足。证据：`src-rust/ops.rs:312-380` `EnrichStats` `enrich_models_with_catalog_with_stats` `enrich_stats_for_ids`；`src-rust/ops.rs:193-200` 日志 `enrich 完成/失败`；`src-rust/tui/app.rs:69` `FetchModelsMessage::Success(EnrichStats)`；`src-rust/web.rs:10` 返回 `{models, enrich}`；`webui/src/components/ProfilesPanel.tsx:48` toast 展示；测试 `ops::enrich_tests::enrich_stats_counts_enriched_skipped_failed` `enrich_stats_with_warning_propagates`；提交 `64c05c1`
- [x] 24h 内多次 Fetch 命中本地缓存，无重复网络请求；过期或网络失败时回退到过期缓存并报告 warning，无缓存且失败时跳过 enrich 并提示 — 满足。证据：`src-rust/catalog.rs:84-129` `get_or_refresh_catalog_inner_with_warning` fresh 命中直接返回 `None` warning；过期回退 `Some(cached), Some(warning)`；测试 `catalog::tests::get_or_refresh_with_warning_fresh_cache_no_warning` `get_or_refresh_with_warning_falls_back_with_warning` `get_or_refresh_with_warning_no_cache_and_fail_returns_warning`；提交 `64c05c1`
- [x] 离线场景（有 24h 内缓存）仍能完成 enrich，无缓存且无网络时保留原值并提示失败原因 — 满足。证据：`src-rust/catalog.rs:84-95` fresh 缓存直接返回；`src-rust/ops.rs:320-330` `catalog=None` 时 `failed=total`；测试 `catalog::tests::get_or_refresh_with_warning_offline_has_cache_still_enriches` `ops::enrich_tests::enrich_stats_offline_with_catalog_still_enriches`；提交 `64c05c1`
- [x] 文案与日志统一使用 CONTEXT.md 术语（模型目录 / 模型元数据），与“上游模型列表”区分 — 满足。证据：`src-rust/catalog.rs:112-128` warning 文案“模型目录拉取失败，回退到过期缓存”；`src-rust/ops.rs:193-240` 日志“模型目录 enrich”；`src-rust/tui/i18n.rs:64` toast “已 enrich...（模型目录未覆盖）”；提交 `64c05c1`
- 追加修复 869edb1：聚合 profile（无 preset）因 `resolve_catalog_provider` 返回 None 导致 skipped 的后续修复，引入全目录全局搜索与 `provider/model` 前缀兼容（`find_global_catalog_model` `normalized_suffix`），保持 per-profile 优先。测试 `ops::enrich_tests::enrich_global_fallback_oc_aggregation_cross_provider` `enrich_global_fallback_prefix_compat` 通过，`cargo test 263 passed`。

## 维度 2 · Spec 遵守
- 满足。实现严格遵循 Implementation Decisions：
  - 模块与 seams：spec 声明“主 seam 为 `ops` 层的 enrich 链，伴随 `config` 新增字段，目录获取封装为 `ops` 私有模块 `catalog`，仍归属主 seam”。当前实现将 `catalog` 拆为独立文件 `src-rust/catalog.rs`（`lib.rs: mod catalog`），但调用关系仍为 `ops` → `catalog`，未暴露独立网络 service 缝，blast radius 未扩大。视为文件组织差异，非决策违背（非阻断观察）。
  - 触发范围：Fetch/发现路径走 enrich，手工 `provider models` 不拦截，`sync_gateway_to_pi` 间接受益 — 代码符合 `src-rust/ops.rs:1135` `upsert_profile` 与异步 `enrich_models_from_catalog`。
  - Provider 映射：显式优先、preset 推断、失败跳过 — 符合。
  - 字段映射与列表语义、未命中策略：均按 Q4 分字段策略实现，extra 等不覆盖。
  - 目录获取与缓存：10s 超时、原子写、TTL 24h、全量缓存 — 符合。
  - 可观测性：EnrichStats 合并到 toast 响应 — 符合。
- Testing Decisions：要求测试位于 `ops` 层 `cargo test`，覆盖字段映射/未命中/TTL/降级/映射优先级/手工不受约束 — 已实现 263 测试，无需为 `web.rs`/TUI 补 UI 测试，符合。
- Out of Scope 检查：未以目录 id 列表替代上游可用性判断（仍以 `profile.models` 为准，目录仅 enrich）；未做增量同步/切片/版本化 diff（始终全量 `api.json`）；未做自动定时后台刷新（仅按需触发）；未接管非网关 provider 自由编辑；未提供目录浏览页（仅输入框）— 均未越过。

## 维度 3 · ADR 遵守
- 0001 tee 流式解析：遵守。未改动 `src-rust/proxy.rs` tee 逻辑。
- 0002 对话边界由客户端标识决定：遵守。未改动 `proxy::conversation_id` 三源逻辑。
- 0003 reasoning 为 output 子集：遵守。enrich 中 `reasoning` 映射为 `Some(bool)`，统计层仍按子集处理，未重复累加。
- 0004 Responses 透传模式：遵守。未改动 `responsesMode` 与透传/转换分支。
- 0005 模型目录为元数据权威源：遵守。`src-rust/catalog.rs` 实现目录拉取与缓存，`src-rust/ops.rs` 按映射 enrich，目录未覆盖保留原值，新增模型需勾选后新增，全量 4.4MB 缓存 24h TTL，失败降级 — 与 ADR 文案一致（`docs/adr/0005-models-dev-as-model-metadata-source.md`）。

## 维度 4 · 文档一致性
- README.md：**非阻断不一致**。功能已实现（模型目录 enrich、Provider 映射输入框、Fetch toast 可观测），但 `README.md` Features 表（`## ✨ Features`）与 `## 📊 Usage Statistics` 等章节未提及模型目录 enrich、模型目录 Provider 字段。未声称未实现功能，无过期信息，但存在功能未文档化。证据：`README.md:6y7-DVa` Features 表 8 行未含 Model Catalog；`webui/src/components/ProfilesPanel.tsx:48` 已实现 `modelsDevProvider` 输入框；`src-rust/tui/i18n.rs:64` 已实现 toast。
- README_ZH.md：同上。
- CONTEXT.md：一致。已新增术语“模型目录”“模型元数据”定义（`CONTEXT.md:193-200`），与 spec/代码文案一致。
- docs/adr：一致。ADR 0005 已创建且与实现一致。
- 建议：后续在 README Features 或 Gateway 相关段落补充一行“Model Catalog enrich”说明，避免用户误以为需手工维护 cost/limit。

## 未满足项清单（显式记录）
- [非阻断] 文档未同步：README.md / README_ZH.md 未描述模型目录 enrich 与 `modelsDevProvider` 输入框。原因：功能 4 票已完成但文档未更新。需要：补充 Features 或 Provider 配置章节一行说明（非阻断，不影响通过）。
- [非阻断] 实现组织与 spec 文案细微差异：spec 描述“目录获取封装为 `ops` 私有模块 `catalog`”，实际为 `src-rust/catalog.rs` 独立文件 `mod catalog`。原因：文件拆分但调用关系未变，blast radius 未扩大。需要：无，或在下次 spec 修订时将文案改为“`catalog` 模块（`src-rust/catalog.rs`）由 `ops` 调用”。

## 待主 agent 隔离验证（L3 条目，如无则写"无"）
- 无。L2 已覆盖 TTL 命中/过期/降级、字段映射、未命中保留等分支，均为纯函数+临时文件隔离，无需真实网络。spec 无“链路可用/端到端”验收项，且 L1 静态核对未发现不一致需升级。注：若需验证真实 `https://models.dev/api.json` 网络链路与 4.4MB 缓存写入，可在隔离 HOME（`TMPDIR`）+ mock 上游环境下由主 agent 执行一次 `cargo test --lib catalog::tests` 已覆盖的集成路径复验。

## 备注
- 证据级别：维度1/2/3 主要 L1 静态（代码位置、提交哈希 e606941/517c269/bde27bf/64c05c1/869edb1/453aeb2）+ 维度1 补充 L2 测试套件（`cargo test --lib` 263 passed, 0 failed, 453aeb2, 2026-08-28 13:32 UTC）。无 L3。
- 本次审计为只读审计，仅新建本报告文件 `.scratch/models-dev-enrich/audit-20260828-2132.md`，未修改任何现有文档与代码。
- 基线：无历史基线，本次为首份报告。
